{
  "name": "web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "gleam:build": "gleam build",
    "build": "gleam build && vite build",
    "lint": "oxlint",
    "preview": "vite preview"
  },
  "devDependencies": {
    "oxlint": "^1.71.0",
    "vite": "^8.1.1"
  }
}
